# Contact form (Resend). Copy to .env.local for dev; set the same in Vercel for prod.
RESEND_API_KEY=re_xxxxxxxxxxxx
CONTACT_TO=you@bogmode.ca
CONTACT_FROM="БОГMODE <form@bogmode.ca>"

# Optional Google Analytics. Leave blank until a GA4 web stream exists.
NEXT_PUBLIC_GA_MEASUREMENT_ID=
# Search Console URL-prefix HTML verification token, if DNS verification is unavailable.
GOOGLE_SITE_VERIFICATION=
